export function Footer() {
  const template = (
    <footer
      style={{ maxWidth: '50rem', margin: 'auto' }}
      className="fixed-bottom text-center"
    >
      <address className="fst-italic">Rodrigo Calvo</address>
    </footer>
  );
  return template;
}
